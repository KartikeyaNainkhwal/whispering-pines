{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd server && npm ci && npx prisma generate && npx tsc prisma/seed.ts --esModuleInterop && npm run build"
    },
    "deploy": {
        "startCommand": "cd server && npx prisma db push --accept-data-loss && npm run db:seed && npm start"
    }
}